apiVersion: v1
kind: Service
metadata:
  name: users-http-service
spec:
  selector:
    app: users-http
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30082
